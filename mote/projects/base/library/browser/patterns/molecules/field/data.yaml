# <label> config
label:
    attrs:
        id: form-field-example-label
        for: form-field-example

# <div> wrapping the input
item:
    tag: div
    children:
        # <input> config
        input:
            id: self.browser.atoms.input
            attrs:
                id: form-field-example
