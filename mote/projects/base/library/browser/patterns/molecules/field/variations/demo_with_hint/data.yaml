# <div> wrapping the input
input:
    children:
        # <input> config
        input:
            attrs:
                aria-describedby: form-field-example-hint

# hint text config
hint:
    attrs:
        id: form-field-example-hint
    value: Hint
